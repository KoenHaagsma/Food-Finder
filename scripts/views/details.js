const Details = {
    render: async () => {
        let view = `
            <section class="section">
                <h1> Details </h1>
            </section>
        `;
        return view;
    },
    after_render: async () => {},
};

export default Details;
