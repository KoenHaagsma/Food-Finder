const Details = {
    render: async () => {
        const view = `
            <section class="section">
                <h1> Details </h1>
            </section>
        `;
        return view;
    },
    after_render: async () => {},
};

// test
export default Details;
